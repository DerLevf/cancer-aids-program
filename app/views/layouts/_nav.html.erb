<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    
    <% if current_user %><%= link_to "Home", debt_projects_path, class: "navbar-brand" %> <% end %>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
          <li class="nav-item">
                <% if current_user %>
                <%= button_to "Abmelden", logout_path, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded mb-4" %>
                

                <%= link_to "Mein Profil", user_path(current_user), class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
                <% end %>

          </li>    
      </ul>
    </div>
  </div>
</nav>