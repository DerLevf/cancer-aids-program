<nav class="navbar">
  <div class="container">
    <% if current_user %>
      <%= link_to "Home", debt_projects_path, class: "navbar-brand" %>
    <% end %>

    <div class="navbar-links">
      <% if current_user %>
        <%= link_to "Aktivitätsprotokolle", activity_logs_path, class: "btn btn-primary" %>
        <%= link_to "Mein Profil", user_path(current_user), class: "btn btn-primary" %>

        <div class="btn-wrapper">
          <%= button_to "Abmelden", logout_path, method: :delete, 
                form: { class: "logout-form" }, 
                class: "btn btn-danger" %>
        </div>
      <% end %>
    </div>
  </div>
</nav>

<style>
/* Navbar Styles */
.navbar {
  background-color: #4a75aa;
  padding: 12px 24px;
  box-shadow: 0 2px 6px rgba(0,0,0,0.1);
}

.navbar .container {
  display: flex;
  justify-content: space-between;
  align-items: center;
  max-width: 1200px;
  margin: 0 auto;
}

.navbar-brand {
  font-size: 1.25rem;
  font-weight: 700;
  color: white;
  text-decoration: none;
}

.navbar-brand:hover {
  color: #dbe6f4;
}

.navbar-links {
  display: flex;
  gap: 12px;
  align-items: center; /* vertical alignment */
}

/* Buttons */
.btn {
  display: inline-block;
  padding: 10px 18px;
  border-radius: 6px;
  font-weight: 600;
  text-align: center;
  text-decoration: none;
  font-size: 0.95rem;
  transition: background-color 0.2s ease;
}

.btn-primary {
  background-color: #3b5a82;
  color: white;
}

.btn-primary:hover {
  background-color: #2f4666;
}

.btn-danger {
  background-color: #f8d7da;
  color: #721c24;
  border: 1px solid #f5c6cb;
}

.btn-danger:hover {
  background-color: #f5c6cb;
}

/* Ensure button_to (which is wrapped in a form) is inline and vertically centered */
.btn-wrapper {
  display: flex;
  align-items: center;
}

.logout-form {
  display: inline; /* prevents full-width form */
  margin: 0;       /* remove extra spacing */
}
</style>
